//Utility.cpp

#include "Utility.h"

//Supporting utility class in order to preform 
//mathematical operations for the 3D graph

class Utility {

	string* Utility::permutations(string toPermute) {
		const int len = toPermute.length();
		string perms[len];
	}
};